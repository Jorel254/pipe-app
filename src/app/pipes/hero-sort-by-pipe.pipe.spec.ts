import { HeroSortByPipePipe } from './hero-sort-by-pipe.pipe';

describe('HeroSortByPipePipe', () => {
  it('create an instance', () => {
    const pipe = new HeroSortByPipePipe();
    expect(pipe).toBeTruthy();
  });
});

import { HeroHexColorPipePipe } from './hero-hex-color-pipe.pipe';

describe('HeroHexColorPipePipe', () => {
  it('create an instance', () => {
    const pipe = new HeroHexColorPipePipe();
    expect(pipe).toBeTruthy();
  });
});

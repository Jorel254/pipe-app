import { HeroFilerPipePipe } from './hero-filer-pipe.pipe';

describe('HeroFilerPipePipe', () => {
  it('create an instance', () => {
    const pipe = new HeroFilerPipePipe();
    expect(pipe).toBeTruthy();
  });
});

import { CanFlyPipePipe } from './can-fly-pipe.pipe';

describe('CanFlyPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CanFlyPipePipe();
    expect(pipe).toBeTruthy();
  });
});
